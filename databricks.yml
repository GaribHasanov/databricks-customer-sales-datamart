bundle:
  name: customer_sales_pipeline

artifacts:
  default:
    type: whl
    path: .

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://<sənin-workspace-url>
