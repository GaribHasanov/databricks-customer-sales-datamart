env: dev

storage:
  base_path: "/Volumes/dev/00_landing_zone"

  datasets:
    customers:
      path: "01_managed_volume/customers"
      format: csv

    sales:
      path: "02_managed_volume/sales"
      format: csv

tables:
  bronze:
    customers: dev_bronze_customers
    sales: dev_bronze_sales

options:
  header: true
  inferSchema: true
